# 2026-03-09

## Operational notes
- Renamed agent `architect` to `leslie` across active configuration and workspace references.
  - Updated `~/.openclaw/openclaw.json`:
    - `agents.list` entry id/name/identity/workspace/agentDir
    - `tools.agentToAgent.allow` from `architect` -> `leslie`
  - Moved directories:
    - `~/.openclaw/workspace-architect` -> `~/.openclaw/workspace-leslie`
    - `~/.openclaw/agents/architect` -> `~/.openclaw/agents/leslie`
  - Updated workspace docs/memory references (`AGENTS.md`, `MEMORY.md`, selected 2026-03-08 logs).
- Restarted gateway to apply changes and verified via `openclaw status --all` that agent now appears as `leslie (leslie)`.
- Leslie governance review completed for proposed new agent `David`; recommendation accepted: pilot first, then promote if metrics pass.
- Implemented pilot agent `david` with constrained PDF/file-op scope:
  - sandbox `all`, workspace access `rw`, scope `agent`
  - tool allowlist: read/write/edit/exec/process/session_status
  - explicit deny for browser, messaging, network/web tools, orchestration, gateway/cron
  - workspace initialized at `~/.openclaw/workspace-david` with guardrail `AGENTS.md`
- Attempted automated Discord channel creation for `#david` via Discord API using configured bot tokens; both default/george accounts returned HTTP 403 (insufficient permission to list/create channels). Pending manual channel creation or permission grant before final routing bind.
- Created Discord channel `#usage` (id `1480677127182614548`) via built-in browser for usage monitoring outputs.
- Re-routed usage alerts (Option 2 + Option 3) to Discord `#usage` by updating prompt payloads and patching cron job messages (`e04d5864...`, `7cfa775f...`).
- Updated design doc at `/mnt/d/Cloud/OneDrive=kernk2@outlook.com/OneDrive/IntamiaTeam/IntamiaApp/03 - Define/OpenClaw/openclaw-usage-monitoring-design-options-1-3.md` to reflect new channel routing.
- Enabled restart continuity baseline using Option 1 + Option 2:
  - Option 1: in-flight task ledger at `state/inflight-task.json`
  - Option 2: restart sentinel/checkpoint at `state/restart-sentinel.json`
- Updated `AGENTS.md` startup + restart continuity protocol, `USER.md` preference note, and `MEMORY.md` governance rule.
- Documented behavior in `/mnt/d/Cloud/OneDrive=kernk2@outlook.com/OneDrive/IntamiaTeam/IntamiaApp/03 - Define/OpenClaw/openclaw-restart-continuity-options-1-2.md`.
- Expanded pilot agent `david` file capabilities to include external CalFresh working directory.
  - Added per-agent sandbox bind: `/mnt/d/Cloud/OneDrive=kernk2@outlook.com/OneDrive/Business/Business2026/00-Projects/apply-CA-assistance-programs/CalFresh/Working-Dir:/data/calfresh-working-dir:rw`.
  - Enabled per-agent sandbox override `sandbox.docker.dangerouslyAllowExternalBindSources=true` (required for non-workspace mount).
  - Updated `workspace-david/AGENTS.md` scope/safety text to allow `/data/calfresh-working-dir`.
  - Verified container mount via `docker inspect`.
- Added stricter restart-task closure enforcement.
  - Updated `AGENTS.md` with mandatory **Closure Gate** after restart-required tasks.
  - Updated `USER.md` preference to require one explicit final closure summary before topic switch.
  - Updated `MEMORY.md` governance rule to include closure summary requirement.
  - Added template file: `state/restart-closure-template.md`.
- Scheduled amoxicillin reminders for next 5 days at 9:59 PM and 7:15 AM (America/Los_Angeles), delivered via isolated main cron jobs that send Telegram reminders directly and auto-delete after run.
  - Created 10 one-shot jobs: PM Mar 9-13 and AM Mar 10-14.
- Added additional amoxicillin reminders at 2:30 PM for the next 5 occurrences (Mar 10–14, local time).
- Updated Option 1 Scheduled Digest cadence to 3x/day (7:00 AM, 1:00 PM, 8:00 PM PT).
  - Cron `usage-option1-daily-digest` updated to `0 7,13,20 * * *` America/Los_Angeles.
  - Prompt updated to 8-hour window and timestamped archive filenames to avoid overwrite.
  - Design doc updated accordingly in OneDrive path.
